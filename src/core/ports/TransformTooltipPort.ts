import type { TooltipPort, TooltipViewHandle, TooltipViewSpec, TooltipViewState } from './TooltipPort.js';

/** @deprecated 内部兼容别名；新交互应使用 `TooltipViewState`。 */
export type TransformTooltipViewState = TooltipViewState;

/** @deprecated 内部兼容别名；新交互应使用 `TooltipViewSpec`。 */
export type TransformTooltipViewSpec = TooltipViewSpec;

/** @deprecated 内部兼容别名；新交互应使用 `TooltipViewHandle`。 */
export type TransformTooltipViewHandle = TooltipViewHandle;

/** @deprecated 内部兼容别名；新交互应使用 `TooltipPort`。 */
export type TransformTooltipPort = TooltipPort;
